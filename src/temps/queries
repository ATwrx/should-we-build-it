// Auth:
///////
const LOGIN = gql `
    mutation Login($email: String $password: String) {
    login(email: $email password: $password){
        token
        user{
        id
        name
        email
        }
    }
}
`;

const SIGNUP = gql `
mutation SignupMutation($name: String $email: String $password: String $role: Role){
  signup(name: $name email: $email password: $password role: $role) {
    token
  }
}
`;

// Project CRUD:
////////////////
const CREATE_DRAFT = gql `
mutation CreateDraftMutation($title: String $text: String $description: String $owner: User){
  createDraft(
    title: $title 
    text: $text
  	description: $description
  )
  {
    id
    owner{
      connection: {
        user() {
        
        }
      }
    }
  }
}
`;

const UPDATE_PROJECT = gql `

`;

const PUBLISH_DRAFT = gql `
 mutation PublishDraft {

 }

`;

// Queries
//////////


const DRAFTS_QUERY = gql`
  query: DraftQuery {

  }
`;

const PROJECT_QUERY = gql `

`;

const USER_QUERY = gql`
  query: UserQuery($id: id) {
    user(where: )
  }
`;

